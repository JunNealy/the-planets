import { planetInfo } from '../../data';
import { useEffect, useState } from 'react';
import './InfoMenu.css';

const InfoMenu = ({ onDetailsChange, selectedPlanet }) => {
  return <div id="info-menu"></div>;
};

export default InfoMenu;
